# Domaine public (HTTPS) – ex: api.btpconnect.fr
PUBLIC_HOST=api.example.com

# JWT
JWT_SECRET=change-me-long-random

# Postgres
POSTGRES_DB=btpconnect
POSTGRES_USER=btpconnect
POSTGRES_PASSWORD=change-me

# CORS (comma-separated origins). Exemple: https://app.example.com,https://api.example.com
CORS_ORIGIN=

# Optional: rate limits
RATE_LIMIT_MAX=300
RATE_LIMIT_TIME_WINDOW=1 minute
